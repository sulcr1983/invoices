# 天颐 · 发票智能处理系统

> 每月几十张发票，手动录入又慢又容易出错？拖进去，3秒出结果。

## 它解决了什么痛苦

财务/行政人员每月要处理大量增值税发票：手动录入发票号码、金额、税号，费时费力还容易出错。查重靠肉眼，归档靠手动建文件夹，统计靠Excel——整个流程又慢又乱。

**天颐**让你把发票文件往里一拖，自动识别、自动去重、自动归档、自动统计，从"手动搬砖"变成"一键搞定"。

## 3个真实场景

**场景1：月底集中报销**
> 小王月底要报销20张发票。以前：逐张打开PDF抄信息到Excel，花1小时。现在：20个文件一起拖进去，点"开始处理"，30秒全部识别入库，台账自动生成。

**场景2：防止重复报销**
> 同一张发票被不同人提交了两次。以前：靠肉眼在Excel里搜发票号。现在：系统自动检测MD5和发票号码重复，重复发票直接拦截，还有审计记录。

**场景3：年底统计汇总**
> 老板要全年发票统计。以前：翻遍所有文件夹手动汇总。现在：打开统计面板，总金额、税额、销售方排名、月度趋势一目了然，还能导出CSV。

## 极简使用（3步搞定）

```
第1步：打开 → 浏览器访问 http://localhost:5000
第2步：拖入 → 把发票文件拖到上传区（支持 PDF、OFD、JPG、PNG）
第3步：点击 → 点"开始处理"，等几秒就完成
```

## 功能清单

| 功能 | 说明 |
|------|------|
| 多格式识别 | PDF、OFD、JPG、PNG 全支持，OFD直接解析XML（最快最准），PDF/图片走百度OCR |
| 智能去重 | 文件MD5 + 发票号码双重去重，重复记录留存审计 |
| 自动归档 | 按年/月目录自动归档，失败文件自动重试 |
| 批量验真 | 勾选多张发票一键调用百度验真接口，逐张返回结果并扣除费用 |
| 企微推送 | 识别成功自动推送企业微信Webhook通知 |
| 金额统计 | 总金额、税额、销售方TOP10、月度处理趋势、部门/费用类型分布 |
| 抵扣预警 | 专票360天抵扣期限自动预警，异常发票高亮提醒 |
| 多维筛选 | 关键词/日期/金额/发票类型/推送状态组合筛选 |
| CSV导出 | 一键导出发票台账为Excel兼容的CSV文件 |
| 异步处理 | 后台线程处理，不阻塞界面操作 |
| 全设备适配 | 电脑、平板、手机均可正常使用 |

## 快速开始

```bash
# 1. 安装依赖
pip install -r system/requirements.txt

# 2. 配置环境变量（可选：百度OCR、企微Webhook）
cp .env.example .env
# 编辑 .env 填入 API Key

# 3. 启动
双击 🚀 一键启动.bat
# 或
python system/api_server.py
```

访问 http://localhost:5000

## Docker 部署

```bash
docker build -t invoice-system .
docker run -d -p 5000:5000 -v ./待识别发票:/app/待识别发票 invoice-system
```

## 目录结构

```
├── 🚀 一键启动.bat         # Windows 启动脚本
├── Dockerfile             # 容器化部署
├── .env.example           # 环境变量模板
└── system/                # 核心代码
    ├── api_server.py      # Flask 应用工厂 + 入口
    ├── config.py          # 全局配置
    ├── main.py            # CLI 入口
    ├── routes/            # Blueprint 路由
    ├── core/              # OCR/解析/流水线
    ├── database/          # SQLite 读写层
    ├── services/          # 文件/发票/同步服务
    ├── components/        # 前端 HTML/CSS/JS
    └── tests/             # 测试用例（55项全通过）
```

## 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| BAIDU_API_KEY | 百度 OCR API Key | 空（跳过OCR，用文本提取兜底） |
| BAIDU_SECRET_KEY | 百度 OCR Secret Key | 空 |
| WECOM_WEBHOOK_URL | 企微 Webhook 地址 | 空（跳过推送） |
| LOG_LEVEL | 日志级别 | INFO |

## 商业价值

- **时间成本**：每张发票从手动录入3分钟 → 自动识别3秒，效率提升60倍
- **错误率**：从人工录入5%错误率 → 机器识别接近0错误
- **合规风险**：重复报销、超期抵扣自动预警，避免税务风险
- **数据资产**：所有发票数据结构化存储，随时可查可导出可分析

## 更新记录

### v6.1 (2026-05-06)

- **批量验真**: 支持勾选多张发票批量调用百度验真，显示费用预估和逐张结果
- **错误日志自动展开**: 处理失败时自动展开日志面板并切换到错误过滤，快速定位问题
- **¥¥ 显示修复**: 修复统计页价税合计金额前重复显示"¥¥"符号的问题
- **风险去重**: 修复异常发票列表中同一发票号重复出现的问题
- **失败详情优化**: 处理失败时展示每个文件的详细错误原因（中文描述）
- **统计登录态校验**: 完善 /api/stats/summary 和 /api/stats/expense-distribution 日期参数过滤

## 测试

```bash
python system/tests/test_cases.py
```
